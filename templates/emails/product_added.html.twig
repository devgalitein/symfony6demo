<!doctype html>
<html lang="en">
<head>

</head>
<body>
<div class="body">
    <div class="container">
        <div class="header text-center">
           Product Details
        </div>
        
        <div class="content">
            <h1>Name: {{ (name is defined) ? name : '' }}</h1>
            <p>Description: {{ (description is defined) ? description:'' }}</p>
            <strong >Price: {{ (price is defined) ? price:'' }}</strong>
        </div>
    </div>
</div>
</body>
</html>
